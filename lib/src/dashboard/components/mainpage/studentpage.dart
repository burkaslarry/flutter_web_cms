import 'package:flutter/material.dart';

class StudentEnrollmentArea extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green[100],
      child: Center(
        child: Text('Student Enrollment Area'),
      ),
    );
  }
}
