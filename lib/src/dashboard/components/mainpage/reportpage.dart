import 'package:flutter/material.dart';

class ReportArea extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow[100],
      child: Center(
        child: Text('Reports Area'),
      ),
    );
  }
}
