import 'package:flutter/material.dart';

class CourseContentArea extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue[100],
      child: Center(
        child: Text('Course Content Area'),
      ),
    );
  }
}
